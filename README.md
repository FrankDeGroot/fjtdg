Folder `swa` is now a Svelte (Vite) frontend deployed to an Azure Static Web App, with folder `api` as a backing Azure Function.

Frontend commands:

- `cd swa && npm install`
- `cd swa && npm run dev`
- `cd swa && npm run dev:local` (runs SWA CLI + API + Vite together)
- `cd swa && npm run build`